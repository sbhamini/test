# Lunch Menu App

A simple Streamlit lunch selection form that emails the selected menu.

## Install dependencies

Use Python 3.9 or newer:

```sh
python -m venv .venv
source .venv/bin/activate
# Windows: .venv\Scripts\activate
pip install -r requirements.txt
